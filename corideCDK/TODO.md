# TODO

- Implement transactional registration/profile update to write both email and phone to `UserContactIndex` alongside `Users` updates. Coordinate with a future `users` DAO to minimize discrepancies.